// script.js
document.addEventListener('DOMContentLoaded', () => {
    const ctaButton = document.getElementById('cta-button');

    if (ctaButton) {
        ctaButton.addEventListener('click', () => {
            alert('Welcome to the X4 Foundations Hub! Explore mods, guides, and more.');
        });
    }
});
